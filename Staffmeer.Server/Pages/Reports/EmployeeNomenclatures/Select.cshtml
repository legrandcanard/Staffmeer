@page
@inject Staffmeer.Server.Models.ApplicationDbContext context

@{
	var items = context.Employees.Select(e => new { e.Id, e.ShortName }).ToList();
	items.Insert(0, new { Id = 0, ShortName = "" });
	var list = new SelectList(items, "Id", "ShortName");
}

<h3>Отчёт</h3>
<h4>Номертклатура, закреплённная за сотрудником</h4>
<hr />
<div class="form-group">
	<label class="control-label">Сотрудник</label>
	<select id="options-list" class="form-control" asp-items="list" autocomplete="off"></select>
</div>

<a id="btnCreateReport" class="btn btn-outline-primary mt-3 disabled" role="button" asp-page="Index" asp-route-id="0"><span class="mdi mdi-file-document-outline"></span> Сформировать</a>

@section Scripts {
	<script src="~/js/reports.js" type="text/javascript"></script>
}